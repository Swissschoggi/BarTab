import SwiftUI

extension Color {
    static let barTabPrimary = Color(
        red: 0x6B / 255,
        green: 0x27 / 255,
        blue: 0x37 / 255
    )

    static let barTabAccent = Color(
        red: 0xC9 / 255,
        green: 0xA2 / 255,
        blue: 0x27 / 255
    )

    static let barTabBackground = Color(
        red: 0xF7 / 255,
        green: 0xF1 / 255,
        blue: 0xE3 / 255
    )

    static let barTabText = Color(
        red: 0x29 / 255,
        green: 0x25 / 255,
        blue: 0x24 / 255
    )

    static let barTabSecondary = Color(
        red: 0xE8 / 255,
        green: 0xDF / 255,
        blue: 0xCC / 255
    )
}
